sudo: false
cache:
  yarn: true
language: node_js
node_js: stable
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.13.0
  - export PATH=$HOME/.yarn/bin:$PATH
install:
  - yarn install
  - yarn bootstrap